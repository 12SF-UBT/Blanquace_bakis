#include<stdio.h>
void main(){
    // The following code calculates the area of a circle.
    /* The float r here is the radius of the circle,
    after putting value of r we can get our results.*/
    float r ;
    float pi = 3.147;
    char character;
    printf("Type the value of radius in cm.");
    scanf("%f",&r);
    
    if( r == character ){
        printf("ERROR");}
    
    else {printf("The area of circle is %f \n cm^2", pi*r*r);}
    
    
    }