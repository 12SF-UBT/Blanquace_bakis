#include <stdio.h>
int main() { /*We will be taking output of a 
  and b then 
  we'll add them */
  int a, b; //int a and b are two random intigers
  printf("Choose a and b \n");
  scanf("%d", &a);
  printf("Choose b \n");
  scanf("%d", &b);
  printf("THe difference is %d", a-b);
  return 0 ;
}